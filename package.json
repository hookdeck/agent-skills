{
  "name": "agent-skills",
  "version": "0.2.0",
  "private": true,
  "description": "Hookdeck agent skills - event-gateway, hookdeck router, outpost",
  "scripts": {
    "test:examples": "./scripts/test-examples.sh",
    "skill:lint": "for d in skills/*/; do tessl skill lint \"$d\" || exit 1; done",
    "skill:review": "for d in skills/*/; do tessl skill review \"$d\" || exit 1; done",
    "validate:plugin": "node scripts/validate-plugin.mjs"
  },
  "devDependencies": {
    "@tessl/cli": "latest"
  }
}
