# Hookdeck webhook signing secret
# Get from: Dashboard → Project Settings → Secrets → Webhook Signing Secret
HOOKDECK_WEBHOOK_SECRET=your_webhook_signing_secret

# Server port (optional, defaults to 3000)
PORT=3000
